-- CreateTable
CREATE TABLE "llm_providers" (
    "id" TEXT NOT NULL,
    "provider" TEXT NOT NULL,
    "apiKey" TEXT NOT NULL,
    "enabled" BOOLEAN NOT NULL DEFAULT true,
    "configuredBy" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "llm_providers_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "llm_providers_provider_key" ON "llm_providers"("provider");

-- CreateIndex
CREATE INDEX "llm_providers_provider_idx" ON "llm_providers"("provider");

-- CreateIndex
CREATE INDEX "llm_providers_enabled_idx" ON "llm_providers"("enabled");
